services:
  marketplace.db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: marketplace_db
    user: root
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=Marketplace_SA_Pass123!
    ports:
      - "14333:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql/data
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P 'Marketplace_SA_Pass123!' -C -Q 'SELECT 1' || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  marketplace.redis:
    image: redis:7-alpine
    container_name: marketplace_redis
    ports:
      - "6379:6379"

  marketplace.seq:
    image: datalust/seq:latest
    container_name: marketplace_seq
    environment:
      - ACCEPT_EULA=Y
      - SEQ_FIRSTRUN_NOAUTHENTICATION=true
    ports:
      - "5341:80"

  marketplace.api:
    image: marketplace-api-dev
    container_name: marketplace_api
    build:
      context: .
      dockerfile: Dockerfile.dev
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_HTTP_PORTS=8080
      - ASPNETCORE_URLS=http://0.0.0.0:8080
      - DOTNET_USE_POLLING_FILE_WATCHER=1
      - DOTNET_WATCH_RESTART_ON_RUDE_EDIT=1
      - ConnectionStrings__DefaultConnection=Server=marketplace.db,1433;Database=MarketplaceDb;User Id=sa;Password=Marketplace_SA_Pass123!;TrustServerCertificate=True;
      - ConnectionStrings__Redis=marketplace.redis:6379
      - JwtSettings__SecretKey=SuperSecretKeyForMarketplaceSecurityService2026!
      - JwtSettings__Issuer=MarketplaceAPI
      - JwtSettings__Audience=MarketplaceClients
      - JwtSettings__ExpiryMinutes=15
      - Seq__ServerUrl=http://marketplace.seq:80
    ports:
      - "5050:8080"
    volumes:
      - .:/app
      - ./uploads:/app/src/Presentation/Marketplace.API/wwwroot/uploads
      - /app/src/Presentation/Marketplace.API/bin
      - /app/src/Presentation/Marketplace.API/obj
      - /app/src/Core/Marketplace.Domain/bin
      - /app/src/Core/Marketplace.Domain/obj
      - /app/src/Core/Marketplace.Application/bin
      - /app/src/Core/Marketplace.Application/obj
      - /app/src/Infrastructure/Marketplace.Infrastructure/bin
      - /app/src/Infrastructure/Marketplace.Infrastructure/obj
      - /app/src/Infrastructure/Marketplace.Identity/bin
      - /app/src/Infrastructure/Marketplace.Identity/obj
      - /app/src/Infrastructure/Marketplace.Scraping/bin
      - /app/src/Infrastructure/Marketplace.Scraping/obj
      - /app/src/SharedKernel/Marketplace.Shared/bin
      - /app/src/SharedKernel/Marketplace.Shared/obj
    depends_on:
      marketplace.db:
        condition: service_healthy
      marketplace.redis:
        condition: service_started
      marketplace.seq:
        condition: service_started

  marketplace.web:
    image: nextjs-web-dev
    container_name: marketplace_web
    build:
      context: ../nextjs-frontend
      dockerfile: Dockerfile.dev
    volumes:
      - ../nextjs-frontend:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NEXT_PUBLIC_API_BASE_URL=http://localhost:5050/api/v1
      - WATCHPACK_POLLING=true
      - CHOKIDAR_USEPOLLING=true
    command: ["npx", "next", "dev", "-p", "3001", "-H", "0.0.0.0", "--webpack"]
    ports:
      - "3000:3001"
    depends_on:
      - marketplace.api

volumes:
  sqlserver_data:
